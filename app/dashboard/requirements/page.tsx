import { RequirementTemplates } from "@/components/dashboard/requirement-templates";

export const metadata = {
  title: "Requirements"
};

export default function RequirementsPage() {
  return <RequirementTemplates />;
}
