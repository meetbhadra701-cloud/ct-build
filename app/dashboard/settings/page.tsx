import { SettingsPanel } from "@/components/dashboard/settings-panel";

export const metadata = {
  title: "Settings - COI Compliance Tracker"
};

export default function SettingsPage() {
  return <SettingsPanel />;
}
